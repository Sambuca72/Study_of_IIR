server 
{
    listen 80;

    location / 
    {
        proxy_pass http://app_cont:8000;
        proxy_set_header Host $host; # передается информация о домене, на который пришел запрос
        proxy_set_header X-Real-IP $remote_addr; # передается реальный айпи юзера, который сделал запрос
    }
}